const system = '你是 SummerFlow AI 学习教练。仅依据给定上下文，不虚构数据；数据不足时明确说明。你无权修改任务、日期、状态、打卡或任何数据，所有内容都只是建议或草稿。建议应具体、可执行，并符合现实时间。'
const jsonOnly = '只返回一个可直接 JSON.parse 的完整 JSON 对象或数组。不要使用 Markdown 代码块、不要附加解释或标题；必须保留所有要求字段。JSON 字段名使用指定英文名，字段值遵循 system 中的输出语言要求。'

export const planPrompt = (request: string, context: unknown) => ({ system, prompt:`根据用户请求创建今天的学习任务草稿。返回 JSON 数组；每项字段为 title、theme、subject、priority（P1/P2/P3）、plannedDuration（分钟）、subtasks（字符串数组）、reason。示例：[ {"title":"完成 STM32 串口实验","theme":"暑期主线学习","subject":"STM32","priority":"P1","plannedDuration":90,"subtasks":["配置串口参数","发送并验证数据"],"reason":"先完成可验证的核心实验"} ]。${jsonOnly}\n用户请求：${request}\n学习上下文：${JSON.stringify(context)}` })
export const breakdownPrompt = (task: unknown, context: unknown) => ({ system, prompt:`拆解任务。返回 JSON 对象：{"suitableForOneDay":true,"warning":"可在今天完成","steps":[{"title":"阅读 STM32 串口配置","estimatedMinutes":30,"detail":"记录关键参数"}]}。${jsonOnly}\n任务：${JSON.stringify(task)}\n学习上下文：${JSON.stringify(context)}` })
export const reviewPrompt = (context: unknown) => ({ system, prompt:`分析今天的学习。返回完整 JSON 对象：{"summary":"今天计划偏多，完成了 8 项任务。","facts":["计划 16 项，完成 8 项，完成率 50%。"],"inferences":["今天可能存在过度规划。"],"suggestions":["将明日核心任务控制在 5～6 项。"]}。数据事实、AI 推断与建议必须分开；不要逐字重复任务或机械罗列原始数组。建议简洁、具体、可执行。数字单位使用中文表达，如“301 分钟”“5 小时 1 分钟”“完成率 50%”。${jsonOnly}\n学习上下文：${JSON.stringify(context)}` })
export const organizeReviewPrompt = (raw: string, context: unknown) => ({ system, prompt:`把用户原始复盘整理为草稿。返回完整 JSON 对象：{"overall":"今天整体节奏较稳定。","achievement":"完成了 STM32 串口实验。","problem":"下午注意力下降。","satisfaction":"按计划完成核心任务。","nextStep":"明天先复现串口收发。"}。不要擅自补充事实，缺失字段使用空字符串。${jsonOnly}\n原始复盘：${raw}\n学习上下文：${JSON.stringify(context)}` })
export const copilotPrompt = (question: string, context: unknown) => ({ system, prompt:`回答学习计划问题。先说明可确认的数据事实，再明确标记你的推断，最后给出不超过 4 条可执行建议。不要声称已经修改任何数据。\n问题：${question}\n学习上下文：${JSON.stringify(context)}` })
